#pragma once

namespace global {
  const unsigned int windowWidth = 800;
  const unsigned int windowHeight = windowWidth;
  const unsigned int blocknumber = 12;
  const float BLOCKSIZE = ((float) windowWidth / (float) blocknumber);
}